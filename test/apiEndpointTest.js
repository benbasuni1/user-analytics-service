const tests = require('mocha'); 
const chai = require('chai');
const chaiHttp = require('chai-http');
const should = chai.should();
chai.use(chaiHttp);

describe('API ENDPOINT', function() {
    it('should ping filter queue', () => {

    });
    it('should insert messages into queue', () => {

    });
})
